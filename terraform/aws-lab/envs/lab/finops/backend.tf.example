terraform {
  backend "s3" {
    bucket       = "your-terraform-state-bucket"
    key          = "lab/finops/terraform.tfstate"
    region       = "us-east-2"
    profile      = "terraform-lab"
    encrypt      = true
    use_lockfile = true
  }
}