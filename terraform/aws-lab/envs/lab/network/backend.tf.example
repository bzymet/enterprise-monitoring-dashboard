terraform {
  backend "s3" {
    bucket       = "your-terraform-state-bucket-name"
    key          = "lab/network/terraform.tfstate"
    region       = "us-east-2"
    profile      = "terraform-lab"
    encrypt      = true
    use_lockfile = true
  }
}